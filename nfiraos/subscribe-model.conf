subsystem = TCS
component = cmNFIRAOS

subscribe {
  events = [
    
    {
      subsystem = NFIRAOS
      component = rtc
      name = telOffloadTt
    }

    {
      subsystem = NFIRAOS
      component = rtc
      name = telOffloadFocus
    }

    {
      subsystem = NFIRAOS
      component = rtc
      name = telOffloadMode
    }

    {
      subsystem = NFIRAOS
      component = rtc
      name = m1Scallop
    }

    {
      subsystem = NFIRAOS
      component = rtc
      name = pwfsPupilShift
    }

    {
      subsystem = NFIRAOS
      component = ngsAdc
      name = imageShift
    }

    {
      subsystem = NFIRAOS
      component = lgsTrombone
      name = state
    }
    {
      subsystem = NFIRAOS
      component = lgsTrombone
      name = target
    }
    {
      subsystem = NFIRAOS
      component = lgsTrombone
      name = current
    }    

    {
      subsystem = NFIRAOS
      component = ngsAdc
      name = state
    }
    {
      subsystem = NFIRAOS
      component = ngsAdc
      name = target
    }
    {
      subsystem = NFIRAOS
      component = ngsAdc
      name = current
    }
    {
      subsystem = NFIRAOS
      component = ngsSsm
      name = state
    }
    {
      subsystem = NFIRAOS
      component = ngsSsm
      name = target
    }
    {
      subsystem = NFIRAOS
      component = ngsSsm
      name = current
    }
    {
      subsystem = NFIRAOS
      component = rtc
      name = state
    }

  ]
}
